import ReactDOM from 'react-dom';
import React from 'react';
import ProcessAbstApp from 'components/ProcessAbstApp';

document.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(
    < ProcessAbstApp />,
    document.getElementById('app')
  );
});
